# frontend/src/styles/

Global CSS — `global.css`. Defines:

- CSS reset
- Color tokens (light + dark theme variables)
- Typography scale
- Shared utility classes (`.scroll-area`, `.card`, ...)

Component-specific styles live in `<Component>.css` files alongside the
component, not here. Use this directory only for things that genuinely
need to be global.
