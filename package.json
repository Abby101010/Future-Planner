{
  "private": true,
  "workspaces": ["backend", "backend/core", "frontend"],
  "scripts": {
    "typecheck": "tsc -b backend/core backend frontend",
    "build:server": "npm --workspace @starward/server run build",
    "build:desktop": "npm --workspace @starward/desktop run build",
    "dev:server": "npm --workspace @starward/server run dev",
    "dev:desktop": "npm --workspace @starward/desktop run electron:dev"
  }
}
